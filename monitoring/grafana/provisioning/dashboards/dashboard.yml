apiVersion: 1

providers:
  - name: OneUI Dashboards
    orgId: 1
    folder: One-UI
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
